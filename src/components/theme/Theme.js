import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#061d34",
    },
    secondary: {
      main: "#f5a000",
    },
  },
});

export default theme;
